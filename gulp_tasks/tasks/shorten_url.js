import gulp from 'gulp'

gulp.task('shorten_url', () => {
/*
  Outline:

  - Create new md file with redirect_to template

  - Detect of user-defined short url is used, or to generate a random one within bounds

  const template = `
  ---
  layout: redirect_to
  title: <OPTIONAL TITLE>
  redirect_to: <URL>
  permalink: /<PATH> <ONLY INCLUDE IF USER DEFINED PATH PROVIDED>
  sitemap: false
  ---
  `;

  - Provide output with a URLs for local and live server (also maybe ngrok?)
  - Make sure this is proper-formed URL
*/
})
